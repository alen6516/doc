# Description
convert - convert between image formats as well as resize an image, blur, crop, despeckle, dither, draw on, flip, join, re-sample, and much more.

# Overview
https://stackoverflow.com/questions/60491688/convert-raw-to-png-with-imagemagick

# Options

# Example
$ convert -size 256x256 -depth 8 rgba:raw_image out.png
    * convert raw_image to png file, need to know the size, color depth and pixel format in advance.
