# Description

# Overview
* to change [tab] into equal size of [space] of a file
* [tab] actually will auto modify it size for text alignment, so the number of [space] will also be auto modified for alignment

# Options
-t NUM
    specify the size of a [tab]

    
# Example
$ expand -t 6 filename

