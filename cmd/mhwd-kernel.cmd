# Description

# Overview

# Options

# Example
$ mhwd-kernel -i linux66
    * install linux 6.6 kernel
    * generate initramfs via mkinitcpio
    * update grub

$ mhwd-kernel -r linux66
    * safely removes the kernel and associated files

$ mhwd-kernel -li
    * list installed kernels

$ mhwd-kernel -l
    * list all available kernels
