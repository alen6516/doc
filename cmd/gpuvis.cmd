# Description

# Overview
* install
    $ sudo apt update
    $ sudo apt install -y cmake libsdl2-dev libfreetype-dev libgtk-3-dev rapidjson-dev g++ trace-cmd
    $ git clone https://github.com/mikesart/gpuvis.git
    $ mkdir gpuvis/build && cd gpuvis/build && cmake -DCMAKE_BUILD_TYPE=Release .. && make

# Options

# Example
