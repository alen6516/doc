# Description

# Overview

# Options

# Example
$ ls -d */
    * list all directories in current path

$ ls -lrt
    * list items reversely sorted by time

$ ls -lrS
    * list items reversely sorted by size
