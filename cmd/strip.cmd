# Description

# Overview

# Options
-d
    Strip debugging symbol only

    
# Example
* $ strip -d a.out
    * strip debugging symbol, a.out can not be debugged line by line
    * but symbol is still there, so still can set breakpoint to main function or else
