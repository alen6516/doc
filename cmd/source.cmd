# Description

# Overview
* `./script.sh` and `bash script.sh` will execute the script in another bash
* `source script.sh` will execute the script in current shell
* if we want `export` command in the script to take effect in current shell, use source

# Options

# Example
