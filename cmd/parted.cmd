# Description

# Overview

# Options

# Example
* Resize the partition size
    $ sudo parted /dev/sdX
    (parted) print
    (parted) resizepart N 100%          # N = partition number
    (parted) quit
