# Description

# Overview
cargo install binary in ~/.cargo/bin, so do:
$ export PATH=~/.cargo/bin/:$PATH

# Options

# Example
$ cargo install amdgpu_top@0.8.5
    * install package of a specific version
