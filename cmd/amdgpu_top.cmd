# Description

# Overview
* install
    $ sudo apt update
    $ sudo apt install -y cargo rustup libdrm-dev
    $ rustup default stable                             # don't add sudo
    $ cargo install amdgpu_top                          # don't add sudo


# Options

# Example
