# Description
SSH file system

# Overview
* To mount a filesystem via ssh:
    * sshfs [user@]host:[dir] mountpoint [options]


# Options

# Example
$ mkdir remote_disk; sshfs USER@HOST:/path/to/folder remote_disk
