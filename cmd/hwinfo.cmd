# Description

# Overview

# Options

# Example
$ hwinfo --monitor
    * check the vendor/model/resolution/frequency of the monitor

$ hwinfo --network
    * check the vender and the driver of the network interface

$ hwinfo --cpu
    * check each core of CPU

$ hwinfo --display
    * check display as well as the driver
