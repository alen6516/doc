# Description

# Overview

# Options
-l
    * show all supported signals

# Example
$ kill -11 $pid
    * send SIGSEGV to pid process

$ killall5 -9
    * forcefully terminate all running processes except your login shell, init, and kernel-specific processes
