# Description

# Overview

# Options

# Example
* install rustc
    $ sudo pacman -S rust
    $ rustup install stable
        * install toolchain
    $ rustup default stable
        * set it as default
    $ cargo --version && rust --version && rustup show
        * verify

