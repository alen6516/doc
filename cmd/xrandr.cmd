# Description

# Overview
* a command line tool to ser/get the size, orientation, reflection of the outputs for a screen
* only works when OS is under graphic mode, use modetest command if in text mode

# Options

# Example
$ xrandr --verbose
    * show current mode and all modes the display support
